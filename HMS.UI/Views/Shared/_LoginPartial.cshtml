@using Microsoft.AspNetCore.Identity
@using HMS.MVC.ViewModels

@*
//////////////////////////////////////////////////
//Author : Mohammed Gaffer Aidaab
//For : King Faisual University
//Under : ISB integrated sulution business Company
//Halls ManageMent System
/////////////////////////////////////////////////////
*@
@{
}

@inject SignInManager<SiteUser> SignInManager
@inject UserManager<SiteUser> UserManager

@if (SignInManager.IsSignedIn(User))
{
    <div class="menu menu-sub menu-sub-dropdown menu-column menu-rounded menu-gray-800 menu-state-bg menu-state-color fw-semibold py-4 fs-6 w-275px show" data-kt-menu="true" style="">
        <!--begin::Menu item-->
        <div class="menu-item px-3">
            <div class="menu-content d-flex align-items-center px-3">
                <!--begin::Avatar-->
                <div class="symbol symbol-50px me-5">
                    <img alt="Logo" src="/assets/media/images/user.png">
                </div>
                <!--end::Avatar-->
                <!--begin::Username-->
                <div class="d-flex flex-column">
                    <a href="#" class="fw-semibold text-muted text-hover-primary fs-7">
                        @{
                            var u = await UserManager.GetUserAsync(User);
                        }
                        <a href="#" class="menu-link px-5">
                            @u.DisplayName
                        </a>
                        @UserManager.GetUserName(User)
                    </a>
                </div>
                <!--end::Username-->
            </div>
        </div>
        <!--end::Menu item-->
        <!--begin::Menu separator-->
        <div class="separator my-2"></div>
        <!--end::Menu separator-->
        <!--begin::Menu item-->
        <div class="menu-item px-5">
            <a href="/Manage/index" class="menu-link px-5" target="iframe_content">
                الملف الشخصي
            </a>
        </div>
        <!--end::Menu item-->
        <!--begin::Menu item-->
        <div class="menu-item px-5">
            <a href="/reservation/index" class="menu-link px-5" target="iframe_content">
                <span class="menu-text">حجوزاتي</span>
                <span class="menu-badge">
                </span>
            </a>
        </div>
        <!--end::Menu item-->
        <!--begin::Menu item-->
       
        <!--end::Menu item-->
        
        <!--begin::Menu separator-->
        <div class="separator my-2"></div>
        <!--end::Menu separator-->
        
        <!--begin::Menu item-->
        <form asp-area="" asp-controller="Account" asp-action="Logout" method="post" id="logoutForm" class="navbar-right">
            <ul class="nav navbar-nav navbar-right d-flex" style="flex-direction:row">
                <div class="menu-item px-5">
                <li class="menu-title">
                    <a class="menu-link px-5" asp-area="" asp-controller="Manage" asp-action="Index" title="Manage">
                        @* <h4>  Hello @UserManager.GetUserName(User)!</h4> *@
                    </a>
                </li>
                <li class="menu-title">
                    <button type="submit" class="btn btn-danger">تسجيل الخروج</button>
                </li>
 
            </ul>
        </form>
    </div>

    <br />
    <br/>
   
}
else
{
    <ul class="nav navbar-nav navbar-right d-flex" style="flex-direction:row">
        <div class="menu-item px-5">
            <li class="menu-title">
                <a class="menu-link px-5" asp-area="" asp-controller="Account" asp-action="Login">
                    تسجيل الدخول
                </a>
            </li>
        </div>
    </ul>
}
